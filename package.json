{
  "name": "oai_client",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./build && npx tsc -p tsconfig.base.json",
    "build-dev": "rm -rf ./build && npx tsc -p tsconfig.dev.json",
    "format": "prettier --write .",
    "start": "node ./build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.2",
    "prettier": "^3.0.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "dotenv": "^16.3.1",
    "gpt-3-encoder": "^1.1.4",
    "openai": "^3.3.0"
  }
}
